import { describe, expect } from '@jest/globals';
import { ethers, isHexString } from "ethers";

/**
 *	unit test
 */
describe( "ECDH", () =>
{
	beforeAll( async () =>
	{
	} );
	afterAll( async () =>
	{
